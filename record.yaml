pcap: true

bag: true

bag_dir: ./recordings/bag

pcap_dir: ./recordings/pcap

bag_args: 

topics: /imu/data /lidar_imu

pcap_args: 

pcap_name: tcpdump_TIMESTAMP.pcap

bag_name: rosbag_TIMESTAMP

date_format: "%Y_%m_%d-%H_%M_%S"
