# not only anymore, but ok

pcap: false

bag: true

bag_dir: ./recordings/bag

pcap_dir: ./recordings/pcap

bag_args: 

topics: /tf /tf_static /fast_limo/body /fast_limo/deskewed /fast_limo/final_raw /fast_limo/match /fast_limo/match_points /fast_limo/original /fast_limo/pointcloud /fast_limo/state /tf /tf_static /imu/data /odometry /lidar_imu /lidar_points /emlid/gps/data /emlid/gps/speed /emlid/navsatfix /clusters /fast_limo/state /Odometry /slam/cones_positions /planning/center_line /planning/visualization_triangulation /planning/center_line_completed /planning/race_status /planning/brake_line /planning/end_line

pcap_args:

pcap_name:

bag_name: rosbag  # rosbag_TIMESTAMP

date_format: "%Y_%m_%d-%H_%M_%S"

enable_ids: true
